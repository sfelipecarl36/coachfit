import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.fitapp.coachfit',
  appName: 'CoachFit',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
